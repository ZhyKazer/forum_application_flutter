name: forum_application_flutter
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.1.5+1
  go_router: ^17.3.0
  cross_file: ^0.3.5+1
  desktop_drop: ^0.7.1
  file_picker: ^11.0.2
  image_picker_gallery_camera: ^0.3.0

  # Supabase
  supabase_flutter: ^2.10.3
  http: ^1.6.0

  # Caching
  hive: ^2.2.3
  crypto: ^3.0.7
  path_provider: ^2.1.6
  google_fonts: ^8.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/data/username_combinations.json
